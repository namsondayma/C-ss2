#include <stdio.h>
int main(){
	float cubic_edge = 4;
	float length = 3;
	float width = 5;
	float radius = 6;
	const float pi = 3.14;
	float square_perimeter =  cubic_edge * 4;
	float area_square = cubic_edge * cubic_edge;
	float perimeter_of_a_rectangle = (length + width) * 2;
	float rectangular_area = length * width;
	float diameter_of_a_circle = 2 * radius * pi;
	float circle_area = radius * radius * pi; 
} 
